includes:
  - layer:launchpad-db
repo: https://git.launchpad.net/launchpad
options:
  ols-pg:
    databases:
      db:
        name: launchpad_dev
        roles:
          - craft-build-job
          - uploader
