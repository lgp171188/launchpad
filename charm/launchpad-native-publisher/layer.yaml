includes:
  - layer:launchpad-db
repo: https://git.launchpad.net/launchpad
options:
  ols-pg:
    databases:
      db:
        name: launchpad_dev
        roles:
          - craft-publish-job
          # TODO: might need uploading role? need to check when testing
