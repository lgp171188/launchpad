includes:
  - layer:launchpad-db
repo: https://git.launchpad.net/launchpad
options:
  apt:
    packages:
      - postgresql-client
  ols-pg:
    databases:
      db:
        name: launchpad_dev
      pgbouncer:
        name: pgbouncer_dev
